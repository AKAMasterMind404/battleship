import 'package:flutter/material.dart';

class PerfectAI extends StatelessWidget {
  static String RouteName = "PerfectAI";

  const PerfectAI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Perfect AI"),
    );
  }
}
