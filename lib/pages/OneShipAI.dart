import 'package:flutter/material.dart';

class OneShipAI extends StatelessWidget {
  static String RouteName = "OneShipAI";

  const OneShipAI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("One Ship AI"),
    );
  }
}
